# Windows

This directory contains the runtime assets, installer scripts, and regression tests for Codex Candy Jelly Skin. End users should run the installer from the parent directory. Developers can run `tests\run-tests.ps1`.
